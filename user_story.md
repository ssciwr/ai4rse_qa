As a researcher, I want to pass the parameters alpha, beta, gamma, delta and
initial conditions x0, y0 as command line arguments to the lotka.py script, so
that I see two plots of the Lotka--Volterra solution: x(t), y(t), and y(x).
